spring:
  jpa:
    hibernate:
      ddl-auto: update # 개발 중에는 update 사용
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/sangsaengpaybackfinder
    username: postgres
    password: mysecretpassword